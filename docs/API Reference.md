# API Reference

This document describes the application programming interface of important modules in the project, such as the `auth.svelte.ts` module.

## Authentication

The `auth.svelte.ts` module provides a set of functions to authenticate users.

To use the module, import it as follows:

```typescript
import { auth } from '$lib/api/auth.svelte';
```

Methods:

